import React from 'react';
import '../App.css'
const Home = () => {
    return (<>
        <div className="home-background">
            <div>
                <h1>Welcome to S K Explore</h1>
                <p>Discover the world with us. You should always include at least one generic family name in a font-family list, since there's no guarantee that any given font is available. This lets the browser select an acceptable fallback font when necessary.</p>
            </div>
        </div>
        </>
    );
};

export default Home;
